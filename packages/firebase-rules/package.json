{
  "name": "firebase-rules",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --project demo-nextfood --only firestore \"vitest run\"",
    "test:unit": "firebase emulators:exec --project demo-nextfood --only firestore \"vitest run\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.1",
    "vitest": "3.2.4"
  }
}
